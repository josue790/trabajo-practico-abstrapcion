from abc import ABC, abstractmethod
class Pago(ABC):
    @abstractmethod
    def procesar_pago(self, monto):
        pass
class TarjetaCredito(Pago):
    def procesar_pago(self, monto):
        return "Pago con tarjeta de " + str(monto)
class PayPal(Pago):
    def procesar_pago(self, monto):
        return "Pago con PayPal de " + str(monto
